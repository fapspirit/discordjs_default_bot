{
  "name": "discordjs-bot",
  "version": "0.1.4",
  "description": "Default discrod bot powered by discord.js",
  "main": "app.js",
  "scripts": {
    "deploy-setup-dev": "pm2 deploy dev setup",
    "deploy-dev": "pm2 deploy dev",
    "deploy-setup-prod": "pm2 deploy production setup",
    "deploy-prod": "pm2 deploy production",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@discordjs/opus": "^0.1.0",
    "bufferutil": "^4.0.1",
    "discord.js": "^12.1.1",
    "libsodium-wrappers": "^0.7.6",
    "request": "^2.81.0",
    "utf-8-validate": "^5.0.2",
    "ytdl-core": "^2.0.1"
  },
  "engines": {
    "node": ">=7.6"
  }
}
